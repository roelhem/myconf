{ config, lib, pkgs, ... }:

{
  home = {
    stateVersion = "23.05";

    packages = [

    ];

    programs = {
      home-manager.enable = true;
    };

    file = {

    };

    sessionVariables = {

    };
  };
}
